<nav class="navbar navbar-default" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <a class="navbar-brand" href="/">Honestbee Mall</a>
    </div>
    <ul class="nav navbar-nav navbar-right">
    <% if current_user %>
      <% if current_user.is_admin? %>
      <li><%= link_to "管理訂單", orders_path %></li>
      <% end %>
      <li><%= link_to "個人資料", current_user %></li>
      <li><a href="<%= destroy_user_session_path %>" data-method="delete"><span class="glyphicon glyphicon-log-out"></span>登出</a></li>
    <% else %>
      <li><a href="<%= new_user_registration_path %>"><span class="glyphicon glyphicon-user"></span>註冊</a></li>
      <li><a href="<%= new_user_session_path %>"><span class="glyphicon glyphicon-log-in"></span>登入</a></li>
    <% end %>
    </ul>
  </div>
</nav>
